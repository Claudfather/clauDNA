# Skills removed by consolidation releases (epic #165, F4: hard removal,
# per-phase releases). One name per line. The removed-names gate in
# validate-skills.py blocks any living repo surface from mentioning these names
# in any reference form. Exempt point-in-time records: CHANGELOG.md and
# documentation/{archive,decisions,planning,specs}/; also skills/cleanup-legacy-install/;
# and `Replaces ...` breadcrumb lines.
#
# P5 (0.11.0) — review engine: /review-work
review-changes
review-pr
#
# P8 (0.13.0) — using-claudna absorbs the install diagnostic
skill-health
#
# P6 (0.10.0) — lenses into the ironclad panel
first-principles
align-to-mission
extension-check
precedent-check
plan-health-audit
cost-benefit
#
# P4 (0.9.0) — session engine: /session
session-handoff
session-resume
name-session
#
# P3 (0.8.0) — audit engine: /audit
security-audit
tech-debt
docs-review
design-review
access-path-audit
data-model-audit
frontend-performance-audit
repo-health
#
# P2 (0.7.0) — infra engines: /modal /railway /vercel /neon
modal-deploy
modal-logs
modal-status
railway-deploy
railway-logs
railway-status
vercel-deploy
vercel-logs
vercel-status
neon-branch
neon-info
neon-query
#
# Vocabulary alignment (#221, epic #197) — /remember renamed /recall
remember
#
# notes/lessons retirement (#204, epic #197) — the global personal store
# (~/.claude/notes/) + per-repo .claude/lessons.md scaffold are retired; memory/
# and the Claudron vault are the knowledge stores.
notes
lessons
#
# Knowledge-surface unification (#202, epic #197) — /learn folds into /capture,
# the one write door. (/claudron capture is absorbed too, but as a subcommand —
# not a top-level skill name — it is repointed to /capture, not gated here.)
learn
#
# Context-aware capture (#203, epic #197) — /reflect folds into /capture's session
# mode: a bare /capture distills the current session's learnings. capture is now
# the sole write door (note / external content / session), scoped by what the note
# is about; the PreCompact hook fires /capture in place of /reflect.
reflect
#
# Pre-ledger hard removals (this file began at epic #165) — deleted in c7c7b03
# ("remove deprecated skills — snowflake-cutover, cache-audit, notifications")
# but never recorded here, so the removed-names gate stayed blind to them (#268).
snowflake-cutover
cache-audit
notifications
#
# Never shipped as skills/<name>/SKILL.md, yet named as live skills in stale docs
# (#268); gated so any future sigiled reference trips the check.
review-self
clauDNA-migrate
#
# Heisted from artemis-skills' upstream evolution of this clauDNA-originated skill —
# /implement-plan renamed /build (challenge round collapses to one adaptive flow,
# no more named 3A/3B split; the multi-phase-epic runner /build-all is new alongside it).
implement-plan
#
# /commit-push-pr and /quick-commit consolidate into /ship (commit mode + PR mode),
# tracking artemis-skills' multi-mode ship host — PR mode gains base-branch rebase,
# a verify-completion gate, review-work diff review, and version/CHANGELOG bump.
commit-push-pr
quick-commit
#
# /visual-crawl consolidates into /qa (adds test-and-fix, report-only, canary,
# benchmark, browse, cookie-setup modes; crawl mode is visual-crawl, carried over).
visual-crawl
